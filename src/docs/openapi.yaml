openapi: 3.0.0
info:
  title: Playenest API
  description: API Documentation for Playenest Beauty GamingCenter Management System
  version: 1.0.0
  contact:
    name: Playenest Support
    email: support@playenest.com

servers:
  - url: /api/v1
    description: V1 API

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    UnauthorizedError:
      description: Access token is missing or invalid
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ForbiddenError:
      description: Insufficient permissions
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    NotFoundError:
      description: The requested resource was not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    ValidationError:
      description: Invalid request payload
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'
    InternalServerError:
      description: Unexpected server error
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  schemas:
    Error:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          type: object
          properties:
            code:
              type: string
              description: Unique error code for machine handling
              enum:
                - INTERNAL_ERROR
                - APP_ERROR
                - VALIDATION_ERROR
                - DB_REQUEST_FAILED
                - NOT_FOUND
                - CONFLICT
                - UNAUTHORIZED
                - FORBIDDEN
                - SLOT_NOT_AVAILABLE
                - BOOKING_OVERLAP
                - OVERLAP_CONFLICT
                - BOOKING_START_TIME_IN_PAST
                - INVALID_TRANSITION
                - ONLINE_BOOKING_DISABLED
                - IDEMPOTENCY_KEY_REQUIRED
                - IDEMPOTENCY_KEY_INVALID_LENGTH
                - IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_PAYLOAD
                - IDEMPOTENCY_REQUEST_IN_PROGRESS
              example: "VALIDATION_ERROR"
            message:
              type: string
              description: Human-readable error message
              example: "Invalid request payload."
            details:
              type: object
              description: Optional additional error details (e.g., validation issues)
              nullable: true
        meta:
          $ref: '#/components/schemas/Meta'

    Success:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: object
        meta:
          $ref: '#/components/schemas/Meta'

    AuthResponse:
      type: object
      properties:
        success:
          type: boolean
        data:
          type: object
          properties:
            accessToken:
              type: string
            refreshToken:
              type: string
            user:
              $ref: '#/components/schemas/User'

    User:
      type: object
      properties:
        id:
          type: string
        phone:
          type: string
        fullName:
          type: string
        role:
          type: string
          enum: [MANAGER, SUPERVISOR, STAFF]
        isActive:
          type: boolean

    GamingCenter:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        slug:
          type: string
        isActive:
          type: boolean
        description:
          type: string
        createdAt:
          type: string
          format: date-time

    GameStation:
      type: object
      properties:
        id:
          type: string
        gamingCenterId:
          type: string
        name:
          type: string
        durationMinutes:
          type: integer
        price:
          type: integer
        currency:
          type: string
        isActive:
          type: boolean

    Reservation:
      type: object
      properties:
        id:
          type: string
        gamingCenterId:
          type: string
        stationId:
          type: string
        staffId:
          type: string
        customerAccountId:
          type: string
        startTime:
          type: string
          format: date-time
        endTime:
          type: string
          format: date-time
        status:
          type: string
          enum: [PENDING, CONFIRMED, COMPLETED, CANCELED, NO_SHOW]
        paymentState:
          type: string
          enum: [UNPAID, PENDING, PARTIALLY_PAID, PAID, REFUNDED, OVERPAID, FAILED, CANCELED]
        (stationSnapshot as any):
          type: string
        (stationSnapshot as any):
          type: integer
        (stationSnapshot as any):
          type: string

    Staff:
      type: object
      allOf:
        - $ref: '#/components/schemas/User'
        - type: object
          properties:
            isPublic:
              type: boolean
            publicName:
              type: string
            bio:
              type: string
            avatarUrl:
              type: string

    Customer:
      type: object
      properties:
        id:
          type: string
        phone:
          type: string
        fullName:
          type: string
        phoneVerifiedAt:
          type: string
          format: date-time

    Meta:
      type: object
      properties:
        requestId:
          type: string
          example: "ckv1abc..."
        pagination:
          type: object
          properties:
            total:
              type: integer
            totalItems:
              type: integer
            page:
              type: integer
            pageSize:
              type: integer
            limit:
              type: integer
            totalPages:
              type: integer

    Pagination:
      type: object
      properties:
        success:
          type: boolean
          example: true
        data:
          type: array
          items:
            type: object
        meta:
          $ref: '#/components/schemas/Meta'

    Settings:
      type: object
      properties:
        id:
          type: string
        gamingCenterId:
          type: string
        timeZone:
          type: string
        workStartTime:
          type: string
        workEndTime:
          type: string
        allowOnlineBooking:
          type: boolean
        onlineBookingAutoConfirm:
          type: boolean

    Commission:
      type: object
      properties:
        id:
          type: string
        reservationId:
          type: string
        gamingCenterId:
          type: string
        status:
          type: string
          enum: [PENDING, ACCRUED, CHARGED, WAIVED, REFUNDED]
        commissionAmount:
          type: integer
        currency:
          type: string

    Rating:
      type: object
      properties:
        id:
          type: string
        gamingCenterId:
          type: string
        reservationId:
          type: string
        rating:
          type: integer
        comment:
          type: string
        status:
          type: string
          enum: [PUBLISHED, HIDDEN, DELETED]

paths:
  /health:
    get:
      summary: Health Check
      tags:
        - System
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/analytics/summary:
    get:
      summary: Get analytics summary
      tags:
        - Analytics
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Analytics summary
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '401':
          $ref: '#/components/responses/UnauthorizedError'

  /gamingCenters/{gamingCenterId}/analytics/staff:
    get:
      summary: Get staff performance analytics
      tags:
        - Analytics
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Staff performance data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/analytics/stations:
    get:
      summary: Get station performance analytics
      tags:
        - Analytics
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: GameStation performance data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/analytics/revenue-chart:
    get:
      summary: Get revenue chart data
      tags:
        - Analytics
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date-time
        - name: endDate
          in: query
          schema:
            type: string
            format: date-time
      responses:
        '200':
          description: Revenue over time data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/settings:
    get:
      summary: Get gamingCenter settings
      tags:
        - Settings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: GamingCenter settings
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    put:
      summary: Update gamingCenter settings
      tags:
        - Settings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                timeZone:
                  type: string
                workStartTime:
                  type: string
                workEndTime:
                  type: string
                allowOnlineBooking:
                  type: boolean
                onlineBookingAutoConfirm:
                  type: boolean
                preventOverlaps:
                  type: boolean
      responses:
        '200':
          description: Settings updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/commissions:
    get:
      summary: List reservation commissions
      tags:
        - Commissions
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: pageSize
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
            enum: [PENDING, ACCRUED, CHARGED, WAIVED, REFUNDED]
      responses:
        '200':
          description: List of commissions
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pagination'

  /gamingCenters/{gamingCenterId}/commissions/{earningId}/pay:
    post:
      summary: Pay a commission (record payment)
      tags:
        - Commissions
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: earningId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - amount
                - currency
              properties:
                amount:
                  type: integer
                currency:
                  type: string
                status:
                  type: string
                  enum: [PENDING, PAID, VOID, REFUNDED]
                  default: PAID
                method:
                  type: string
                  enum: [CASH, CARD, ONLINE, TRANSFER]
                referenceCode:
                  type: string
                paidAt:
                  type: string
                  format: date-time
      responses:
        '201':
          description: Commission payment recorded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /public/gamingCenters/{gamingCenterSlug}/pages/{pageSlug}:
    get:
      summary: Get a public page by slug
      tags:
        - CMS
      parameters:
        - name: gamingCenterSlug
          in: path
          required: true
          schema:
            type: string
        - name: pageSlug
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Page details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/media/upload:
    post:
      summary: Upload an image
      tags:
        - CMS
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                purpose:
                  type: string
                  enum: [COVER, GALLERY, BEFORE_AFTER, LOGO]
                altText:
                  type: string
      responses:
        '201':
          description: Image uploaded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/customers:
    get:
      summary: List customers for a gamingCenter
      tags:
        - Customers
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: limit
          in: query
          schema:
            type: integer
      responses:
        '200':
          description: List of customers
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pagination'
    post:
      summary: Create or link a customer
      tags:
        - Customers
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
              properties:
                phone:
                  type: string
                fullName:
                  type: string
                displayName:
                  type: string
                note:
                  type: string
      responses:
        '201':
          description: Customer created or linked
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /public/gamingCenters/{gamingCenterSlug}/reservations:
    post:
      summary: Create an online reservation
      tags:
        - Bookings
      parameters:
        - name: gamingCenterSlug
          in: path
          required: true
          schema:
            type: string
        - name: idempotency-key
          in: header
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - stationId
                - staffId
                - startTime
                - customer
              properties:
                stationId:
                  type: string
                staffId:
                  type: string
                startTime:
                  type: string
                  format: date-time
                customer:
                  type: object
                  required:
                    - fullName
                    - phone
                  properties:
                    fullName:
                      type: string
                    phone:
                      type: string
                    email:
                      type: string
                note:
                  type: string
      responses:
        '201':
          description: Reservation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          description: Bad Request (e.g. Reservation in the past)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '403':
          description: Forbidden (e.g. Online reservation disabled)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Conflict (e.g. Slot not available)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /gamingCenters/{gamingCenterId}/reservations:
    get:
      summary: List reservations for a gamingCenter
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: page
          in: query
          schema:
            type: integer
        - name: pageSize
          in: query
          schema:
            type: integer
        - name: status
          in: query
          schema:
            type: string
            enum: [PENDING, CONFIRMED, COMPLETED, CANCELED, NO_SHOW]
      responses:
        '200':
          description: List of reservations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pagination'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
    post:
      summary: Create a reservation (Panel)
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - stationId
                - staffId
                - startTime
                - customer
              properties:
                stationId:
                  type: string
                staffId:
                  type: string
                startTime:
                  type: string
                  format: date-time
                customer:
                  type: object
                  required:
                    - fullName
                    - phone
                  properties:
                    fullName:
                      type: string
                    phone:
                      type: string
                note:
                  type: string
      responses:
        '201':
          description: Reservation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '400':
          $ref: '#/components/responses/ValidationError'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Conflict (e.g. Slot not available)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /gamingCenters/{gamingCenterId}/reservations/{reservationId}:
    get:
      summary: Get reservation by ID
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Reservation details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    patch:
      summary: Update reservation details
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                stationId:
                  type: string
                staffId:
                  type: string
                startTime:
                  type: string
                  format: date-time
                note:
                  type: string
      responses:
        '200':
          description: Reservation updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/reservations/{reservationId}/confirm:
    post:
      summary: Confirm a reservation
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Reservation confirmed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Conflict (e.g. Invalid state transition)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /gamingCenters/{gamingCenterId}/reservations/{reservationId}/cancel:
    post:
      summary: Cancel a reservation
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Reservation canceled
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
        '401':
          $ref: '#/components/responses/UnauthorizedError'
        '404':
          $ref: '#/components/responses/NotFoundError'
        '409':
          description: Conflict (e.g. Invalid state transition)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /gamingCenters/{gamingCenterId}/reservations/{reservationId}/complete:
    post:
      summary: Complete a reservation
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Reservation completed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/reservations/{reservationId}/no-show:
    post:
      summary: Mark reservation as no-show
      tags:
        - Bookings
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: reservationId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Marked as no-show
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/staff:
    get:
      summary: List staff for a gamingCenter
      tags:
        - Staff
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of staff
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    post:
      summary: Add new staff member
      tags:
        - Staff
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - fullName
                - phone
                - role
              properties:
                fullName:
                  type: string
                phone:
                  type: string
                role:
                  type: string
                  enum: [MANAGER, SUPERVISOR, STAFF]
                isPublic:
                  type: boolean
                publicName:
                  type: string
                bio:
                  type: string
                avatarUrl:
                  type: string
      responses:
        '201':
          description: Staff member added
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/staff/{userId}/staffShifts:
    get:
      summary: List staffShifts for a staff member
      tags:
        - Shifts
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of staffShifts
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    post:
      summary: Upsert staffShifts for a staff member
      tags:
        - Shifts
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
        - name: userId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                type: object
                required:
                  - dayOfWeek
                  - startTime
                  - endTime
                  - isActive
                properties:
                  dayOfWeek:
                    type: integer
                  startTime:
                    type: string
                  endTime:
                    type: string
                  isActive:
                    type: boolean
      responses:
        '200':
          description: Shifts updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{gamingCenterId}/stations:
    get:
      summary: List stations for a gamingCenter
      tags:
        - Services
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of stations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    post:
      summary: Create a new station
      tags:
        - Services
      security:
        - bearerAuth: []
      parameters:
        - name: gamingCenterId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - durationMinutes
                - price
                - currency
              properties:
                name:
                  type: string
                durationMinutes:
                  type: integer
                price:
                  type: integer
                currency:
                  type: string
                isActive:
                  type: boolean
      responses:
        '201':
          description: GameStation created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /public/gamingCenters/{gamingCenterSlug}/stations:
    get:
      summary: List public stations for a gamingCenter
      tags:
        - Services
      parameters:
        - name: gamingCenterSlug
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: List of public stations
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters:
    get:
      summary: List all gamingCenters
      tags:
        - GamingCenters
      responses:
        '200':
          description: List of gamingCenters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Pagination'
    post:
      summary: Create a new gamingCenter
      tags:
        - GamingCenters
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - name
                - slug
                - ownerId
              properties:
                name:
                  type: string
                slug:
                  type: string
                ownerId:
                  type: string
      responses:
        '201':
          description: GamingCenter created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /gamingCenters/{id}:
    get:
      summary: Get gamingCenter by ID
      tags:
        - GamingCenters
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: GamingCenter details
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    patch:
      summary: Update gamingCenter
      tags:
        - GamingCenters
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                slug:
                  type: string
                isActive:
                  type: boolean
      responses:
        '200':
          description: GamingCenter updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
    delete:
      summary: Delete gamingCenter
      tags:
        - GamingCenters
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: GamingCenter deleted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /auth/user/otp/request:
    post:
      summary: Request OTP for User
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
              properties:
                phone:
                  type: string
                  example: "09123456789"
      responses:
        '200':
          description: OTP sent
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /auth/user/otp/verify:
    post:
      summary: Verify OTP and get Token
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
                - code
              properties:
                phone:
                  type: string
                  example: "09123456789"
                code:
                  type: string
                  example: "123456"
      responses:
        '200':
          description: Verified
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'
        '401':
          description: Invalid or expired OTP
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /auth/login:
    post:
      summary: Classic Login (Email/Password)
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
                - actorType
              properties:
                phone:
                  type: string
                password:
                  type: string
                actorType:
                  type: string
                  enum: [USER, SYSTEM_ADMIN]
                gamingCenterId:
                  type: string
      responses:
        '200':
          description: Logged in
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/user/login/otp:
    post:
      summary: Login with OTP
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - phone
                - gamingCenterId
              properties:
                phone:
                  type: string
                  example: "09123456789"
                gamingCenterId:
                  type: string
      responses:
        '200':
          description: Logged in
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/refresh:
    post:
      summary: Refresh Token
      tags:
        - Auth
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - refreshToken
              properties:
                refreshToken:
                  type: string
      responses:
        '200':
          description: Token refreshed
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuthResponse'

  /auth/logout:
    post:
      summary: Logout
      tags:
        - Auth
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Logged out
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'

  /auth/me:
    get:
      summary: Get current user info
      tags:
        - Auth
      security:
        - bearerAuth: []
      responses:
        '200':
          description: User info
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Success'
